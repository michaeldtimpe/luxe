"""Vendored from https://github.com/alexziskind1/codeneedle bench/.

Modules: extract.py (esprima-based AST + Python ast.parse function enumerator),
scorer.py (SequenceMatcher-based line alignment + tagging), runner.py
(reference; not imported — we build our own runner using luxe Backend).
"""
