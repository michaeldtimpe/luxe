# the-game

Personal movie randomizer (per repo description). The repo is currently **empty** — only `.git` + `.gitignore` exist.

## Stack

Not yet chosen.

## Entry points

None.

## Dev loop

N/A.

## Tests / lint

None.

## Agent gotchas

- **Blank slate.** Any work starts from scratch — pick a stack before writing code.
- **`.gitignore` is pre-provisioned for secrets.** `*.key`, `*.pem`, `secrets.json` are already blocked. When adding config files, respect this pattern.
- **Purpose is a movie randomizer.** Keep scope tight; avoid over-engineering a weekend project into infrastructure.
