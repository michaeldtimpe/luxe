{
  "name": "retrieve_oracle_whole_file",
  "description": "Oracle retrieval, whole-file output. Isolates whether the format change alone suffices, independent of retrieval.",
  "stages": [
    { "id": "preprocess", "enabled": false },
    { "id": "index", "enabled": false },
    { "id": "retrieve", "enabled": true, "params": { "method": "oracle" } },
    { "id": "compress", "enabled": false },
    { "id": "prompt_assembly", "enabled": true, "params": { "output_format": "whole_file" } }
  ]
}
