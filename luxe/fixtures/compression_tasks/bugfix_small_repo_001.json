{
  "id": "bugfix_small_repo_001",
  "repo_id": "example_small_repo",
  "description": "Fix the failing unit tests in tests/test_math.py related to negative inputs. sum_all currently drops negative numbers; it should include them.",
  "entry_instruction": "You are helping fix a bug in this repository.",
  "goal": "All tests in tests/test_math.py pass when running `pytest -q tests/test_math.py` from the repository root.",
  "validation": {
    "type": "pytest",
    "command": ["pytest", "-q", "tests/test_math.py"],
    "success_criteria": { "exit_code": 0 }
  },
  "reference": {
    "relevant_files": ["mathops.py"]
  }
}
